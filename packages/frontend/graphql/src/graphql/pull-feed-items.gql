query PullFeedItems($pullInput: [PullFeedsInput!]!) {
  pullFeedItems(pullInput: $pullInput) {
    feedId
    feedItemId
    title
    link
    descriptionMarkdown
    contentMarkdown
    createdAt
  }
}
