import { style } from '@vanilla-extract/css';

export const loadingIcon = style({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
});

export const feedIconContainer = style({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  width: '1em',
  height: '1em',
});
